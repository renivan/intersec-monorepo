[package]
name = "flow-engine"
version = "1.0.0"
edition = "2024"

[lib]
name = "flow_engine"
path = "src/lib.rs"

[dependencies]
core-types = { path = "../core-types" }
protocol-engine = { path = "../protocol-engine" }