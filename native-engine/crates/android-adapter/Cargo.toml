[package]
name = "android-adapter"
version = "0.1.0"
edition = "2021"

[lib]
name = "android_adapter"
path = "src/lib.rs"

[dependencies]
ffi-contracts = { path = "../ffi-contracts" }
kotlin-bridge-contracts = { path = "../kotlin-bridge-contracts" }
