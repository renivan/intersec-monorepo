[package]
name = "security-dissector"
version = "0.1.0"
edition = "2021"

[lib]
name = "security_dissector"
path = "src/lib.rs"

[dependencies]
core-types = { path = "../core-types" }
protocol-engine = { path = "../protocol-engine" }
log = "0.4"
