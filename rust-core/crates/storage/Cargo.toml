[package]
name = "storage"
version = "0.1.0"
edition = "2021"

[lib]
name = "storage"
path = "src/lib.rs"

[dependencies]
session-manager = { path = "../session-manager" }
use-cases = { path = "../use-cases" }