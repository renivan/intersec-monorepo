  [package]
name = "kotlin-bridge-contracts"
version = "0.1.0"
edition = "2021"

[lib]
name = "kotlin_bridge_contracts"
path = "src/lib.rs"

[dependencies]
# Removidas dependências circulares para permitir que o adaptador dependa dos contratos

